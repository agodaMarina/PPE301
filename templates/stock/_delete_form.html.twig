<form method="post" action="{{ path('delete_stock', {'id': stock.id}) }}" onsubmit="return confirm('Etes-vous sûr de supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock.id) }}">
    <button class="btn btn-warning mt-2">Supprimer</button>
</form>
